name = "pages-cron-deploy"
main = "index.js"
compatibility_date = "2026-09-08"
workers_dev = false

# Daily at 00:00 UTC. Matches the live Worker. Do not go back to 6-hourly.
[triggers]
crons = ["0 0 * * *"]
